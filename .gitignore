node_modules

.idea/

# ZCode workspace skills are machine-specific absolute-path symlinks
.zcode/

# WorkBuddy worktree-local session state (logs, scratch) — tool data, not project content
.workbuddy/
